---
image: ubuntu:latest
before_script:
  - /bin/sh -c 'wget https://github.com/earthly/earthly/releases/download/v0.6.8/earthly-linux-amd64 -O /usr/local/bin/earthly && chmod +x /usr/local/bin/earthly'

stages:
  - build

build-job:
  stage: build
  script:
  - cat /proc/cpuinfo
  - cat /etc/os-release
  - earthly -P +examples

